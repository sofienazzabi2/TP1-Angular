import { CustomColorsDirective } from './custom-colors.directive';

describe('CustomColorsDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomColorsDirective();
    expect(directive).toBeTruthy();
  });
});
